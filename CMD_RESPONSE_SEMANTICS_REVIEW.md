# CMD Response 语义审核（基于最新抓包）

数据来源：
- `/Users/xiasiyi/GolandProjects/stzbHelper/logs/stzbHelper.log`
- MySQL `stzb.cmd_schema`、`stzb.cmd_field_schema`

采样时间：`2026-03-19 19:53`（北京时间）。

## 1) 已可确认（高置信）

| cmd_id | 建议中文语义 | 请求类型 | 证据（response 形态） |
|---|---|---|---|
| 100 | 同盟总览信息 | 主界面状态拉取 | `[0,[...,{union大对象...}]]`，字段含 `union_id/name/leader_id/total_member/notice` |
| 142 | 同盟分组元数据 | 配置拉取 | `[[group_id,group_name,leader_id,leader_name,member_count,power,...], ...]` |
| 143 | 成员分组映射 | 配置拉取 | `[[member_id,group_id,group_name], ...]` |
| 700 | 同盟排行榜 | 排行榜拉取 | `[...,[ [rank,{name,power,total_member,total_npc_city,union_id,...}], ... ]]` |
| 949 | 成员状态增量 | 推送增量 | `[0/1,[id...],[[wid,group,union,ts,...,name,...]],...]` |
| 514 | 玩家外观批量快照 | 批量快照 | `[obj_id,[skin_or_title,param,...], obj_id2,[...], ...]`（非“个人排行榜”） |
| 947 | 玩家外观增量 | 推送增量 | `[op,wid,...,name,ts,appearance_id,"x,y",...]` |
| 3758 | 同盟通告 | 列表拉取 | `[[notice_id,author,title,ts],...]` |
| 6231 | 查看战报具体信息 | 详情拉取 | `[0,{battle_id,brief_report,...}]` |
| 99991 | 登录界面 | 登录态拉取 | `[3,[[role_key,role_name,role_id],...],[]]` |

## 2) 待你截图确认（中置信）

| cmd_id | 当前推断 | 请求类型 | 现有证据 |
|---|---|---|---|
| 135 | 城池/州府占领态势列表 | 快照拉取 | 样本含大量 `["延津",4,0,7460868,9,...]` 城池结构 |
| 6314 | 据点关系/队伍连接快照 | 快照拉取 | 行结构 `[event_id,group_id,"成员ID串"]`，与 6317 同步变化 |
| 6317 | 6314 的增量触发/变更通知 | 推送增量 | 多为 `[event_id]` 或 `[event_id,event_id...]` |
| 6319 | 个人/队伍事件播报 | 推送增量 | 样本 `["昵称",13,19,10,7260857,1]` |
| 90005 | 通用表增量推送（Tb_*） | 推送增量 | `[[2/3,"Tb_user_build",[...]], ...]` |
| 900 | wid批量请求/订阅列表 | 请求参数回显或拉取 | 纯 wid 数组 `[6920752,7530931,...]` |
| 724 | 角色详情列表（含签名/州/称号） | 列表拉取 | `[[wid,[...,"签名",...,name,union,...]],...]` |
| 725 | 联盟关系与分组视图 | 列表拉取 | `[wid,[camp_id,camp_name,...,group_names...],[],{}]` |

## 3) 明确纠偏

- `103`：仍是你原始项目主链路“同盟成员数据”协议，但本轮样本里暂未抓到新包；当前实现已回到“仅 103 入 team_user 主链路”。
- `514`：从最新样本看更像“玩家外观/形象快照”，不应继续标成“个人排行榜”。

## 4) 你给截图时请按这个清单

每次只做一个动作，然后把动作名和截图发我，我来对时序匹配 `cmdid`：

1. 打开“同盟成员列表”页（不点分组）
2. 切换成员分组（百里/茶/真武等）
3. 打开“个人主页/名片”（任意成员）
4. 打开“城池/打城”相关列表页
5. 打开“排行榜-个人”页
6. 打开“排行榜-同盟”页
7. 打开“战报详情”页

## 5) 你确认后我下一步会做

1. 把高置信映射批量写入 `cmdNameCN + cmd confirm`。
2. 给 `6314/6317/6319/90005` 增加专门解析器（至少先落结构化表）。
3. 在前端确认页新增“动作-截图-命中cmd”时间轴，便于你肉眼二次校验。
