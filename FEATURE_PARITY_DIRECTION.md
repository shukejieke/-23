# stzbtool 功能复现技术方向（stzbHelper）

> 文档目标：为“先复现 [stzbtool 功能页](http://stzbtool.cn/download/features/)”提供可执行技术路线。
>
> 说明：当前环境无法直接抓取该页面内容，下面先给出“对标框架 + 现有能力基线 + 实施计划”。拿到目标站功能清单（截图或文本）后可在本方案上直接收敛为精确里程碑。

---

## 1. 目标定义

### 1.1 总目标
- 在现有 `stzbHelper` 架构上，分阶段实现与目标站“核心业务功能一致”（先能力一致，再交互细节一致）。

### 1.2 对齐原则
- 功能优先级：`数据正确性 > 可用性 > 性能 > UI 还原度`。
- 协议侧优先：先保证抓包/解析/入库稳定，再做前端交互补齐。
- 渐进式改造：继续沿用 `handler -> service -> repo` 分层，避免回到巨石模式。

---

## 2. 当前能力基线（已具备）

基于当前仓库代码，已有能力：

- 同盟成员：查询、分组、导出
- 攻城任务：创建、拉取战报、统计主力/拆迁、导出
- 分组武勋：按组统计人数/总武勋/均值/0武勋
- 队伍查询：基于详细战报的队伍聚合查询与导出
- 排行榜：同盟榜（700）+ 个人榜（514）入库与看板
- 运行时诊断：抓包/解析/cmd 统计状态接口

可参考：
- [/Users/xiasiyi/GolandProjects/stzbHelper/web/src/pages/Index.vue](/Users/xiasiyi/GolandProjects/stzbHelper/web/src/pages/Index.vue)
- [/Users/xiasiyi/GolandProjects/stzbHelper/docs/API_DATA_MAP.md](/Users/xiasiyi/GolandProjects/stzbHelper/docs/API_DATA_MAP.md)
- [/Users/xiasiyi/GolandProjects/stzbHelper/docs/ARCHITECTURE.md](/Users/xiasiyi/GolandProjects/stzbHelper/docs/ARCHITECTURE.md)

### 2.1 根据功能页截图的首版对标矩阵（2026-03-19）

> 说明：以下是基于你提供截图做的“能力映射”（不含目标站二级页面细节）。

| 模块 | 目标站描述（截图） | 当前状态 | 主要差距 |
|---|---|---|---|
| 自动战报 | 自动抓取同盟战报、滚动翻页、暂停/终止 | 部分完成 | 仍以手动触发为主，缺少任务化自动滚动/暂停状态机 |
| 排行榜 | 个人/联盟、历史对比、各州统计、全区团队排行 | 部分完成 | 已有 514/700 看板；缺历史对比、分州统计、跨区维度 |
| 打城功能 | 智能排表、考勤统计、数据导出 | 部分完成 | 已有任务/统计/导出；缺“智能排表”与排期策略 |
| 武勋统计 | 每日/每周/赛季、势力分析 | 部分完成 | 当前偏周维度分组统计；缺日/赛季分层和趋势分析 |
| 团数据 | 全盟统计、团队对抗、团报导出 | 未开始 | 缺“团”实体模型、对抗指标与专用报表 |
| 战场报告 | 看板、可视化图表 | 部分完成 | 有战报列表查询；缺图表化仪表盘与多维分析面板 |
| 队伍阵容 | 主力看板、阵容分析、对战胜率 | 部分完成 | 有队伍查询；缺阵容评分、胜率统计与推荐视图 |
| 自定义配置 | 主力判定、敌方分组、积分配置 | 未开始 | 缺统一配置中心与策略配置持久化 |
| 数据库 | 本地管理、云端同步、多设备协作 | 未开始 | 当前本地 MySQL；缺同步协议、账号空间、多端一致性 |

### 2.2 建议 P0/P1/P2（按投入产出）

- `P0`：自动战报、打城功能、武勋统计、战场报告、队伍阵容  
  原因：这些与你当前抓包与战报链路直接相关，改造成本最低、最能形成“可用产品”。
- `P1`：排行榜增强、团数据、自定义配置  
  原因：有基础但需要新增模型和统计口径。
- `P2`：数据库云同步/多端协作  
  原因：工程复杂度最高，需账号与同步架构设计。

---

## 3. 功能复现方法（建议流程）

### 3.1 先做“功能对标台账”
- 从目标站逐项整理：功能名称、入口位置、输入参数、输出字段、刷新机制、导出格式、限制条件。
- 每项定义验收标准：至少 1 条“可量化”标准（如“筛选 3 条条件后结果一致”）。

建议台账字段：
- `模块`
- `目标能力描述`
- `现状（已完成/部分完成/未开始）`
- `后端差距`
- `前端差距`
- `协议/数据差距`
- `优先级（P0/P1/P2）`
- `验收方式`

### 3.2 再做“后端先行补齐”
- 抓包分发和解析优先保证稳定落库。
- API 返回结构固定化（避免前端反复适配）。
- 对高频查询建索引和分页约束（避免页面卡顿）。

### 3.3 最后做“页面体验对齐”
- 路由、筛选器、表格字段、导出文档对齐。
- 保持已有业务语义，不做“错误兜底造数”。

---

## 4. 分阶段落地计划

### Phase A：对标冻结（1-2 天）
- 输出《目标站功能台账 v1》
- 标注 P0（必须先复现）功能
- 给出“现状差距图”和执行顺序

交付物：
- `docs/FEATURE_PARITY_GAP_TRACKER.md`

### Phase B：P0 能力闭环（3-7 天）
- 补齐 P0 的解析链路/API/前端页面
- 加入运行时诊断指标，确保可排障
- 完成最小回归测试（API + 关键 SQL）

交付物：
- 可运行版本 + 回归清单

建议拆分：
- B1 自动战报任务化（开始/暂停/终止 + 进度）
- B2 打城与武勋统计增强（日/周/赛季口径）
- B3 战场与队伍看板图表化（可视化胜率/阵容）

### Phase C：P1/P2 完善（持续迭代）
- 高级筛选、导出格式一致化、交互细节
- 性能优化（分页、索引、缓存）
- 文档与变更记录同步

---

## 5. 架构方向建议（延续现有重构）

### 5.0 被动采集优先策略（先解决“必须进游戏触发”）
- 事实：当前数据来源是抓包，无法像普通 REST API 一样“后端主动二次请求”。
- 正确方向：把产品从“主动拉取”改为“被动采集编排”。
- 第一阶段目标：
  - 给每类数据增加“最后采集时间/过期状态”
  - 明确返回“建议去游戏触发的页面”
  - 前端据此引导用户操作，而不是盲目刷新

已接入接口（2026-03-19）：
- `GET /v1/stzb/runtime/freshness`
- `GET /v1/stzb/runtime/status`（内含 `dataset_freshness`）

### 5.1 数据链路稳定化
- `capture`：继续做协议噪声过滤与分发监控
- `parser`：按 `cmd_id` 分文件拆分，降低协议漂移影响面
- `repo`：统一参数化查询，避免 SQL 注入与方言兼容问题

### 5.2 观测与排障标准化
- 保持 `runtime/status` 与 `runtime/cmd/list`
- 为核心链路补充计数器：解析成功率、落库失败率、接口耗时分位

### 5.3 一致性策略
- 禁止“伪数据兜底”污染旧语义
- 对每个接口定义字段契约，避免前后端理解偏差

### 5.4 CMD 语义自动化闭环（已落地）
- 目标：解决“抓包可见但语义未知”的 `cmd_id` 识别效率问题。
- 实现方式：
  - 后端分析接口基于 `sample_text` / 字段路径 / 历史命名，自动推断：
    - `semantic_guess`（语义猜测）
    - `suggested_name`（建议中文名）
    - `request_type`（全量快照/增量推送/事件通知）
    - `confidence`、`basis`（证据）
  - 前端提供“CMD语义确认中心”用于人工复核与一键回写。
- 现有接口：
  - `GET /v1/stzb/runtime/cmd/analysis`
  - `POST /v1/stzb/runtime/cmd/confirm`
- 使用建议：
  - 每次赛季或版本更新后，先打开确认页按 `only_unknown=1` 过滤，优先处理未知 `cmd_id`。
  - 对低置信度项先观察 2-3 轮样本，再确认写回，避免误标污染统计口径。

---

## 6. 主要风险与规避

- 协议漂移：`cmd_id` 语义变化导致页面空数据  
  规避：保留 cmd 观测、样本落库、快速开关

- 抓包权限：macOS `/dev/bpf*` 权限导致无法采集  
  规避：启动自检 + 明确错误提示

- 数据质量：解析失败或错误兜底导致统计失真  
  规避：严格类型校验 + 失败日志 + 回滚策略

- 查询性能：战报表增大后查询变慢  
  规避：索引、分页上限、条件裁剪

---

## 7. 接下来我可以直接帮你做什么

1. 先产出“可执行对标表”  
我可以基于你提供的目标站截图/功能清单，直接生成 `P0/P1/P2` 差距台账和开发顺序。

2. 直接开工复现 P0 功能  
你指定一个模块（例如“攻城任务”或“队伍查询”），我可以从后端到前端一次性补齐并自测。

3. 建立回归检查清单  
我可以落地一份 `API + SQL + 页面` 的回归脚本清单，确保每次改动不把已有功能改坏。

4. 做发布前收敛  
我可以统一版本号、补变更日志、输出“可发布检查报告”。

---

## 8. 你给我这两样，我能马上进入精确复现

- 目标站功能页截图（全页或分模块都行）
- 你定义的优先级（先做哪 2-3 个模块）

收到后我会直接输出：
- 精确 gap 分析
- 迭代计划（按天）
- 第一批代码改动
